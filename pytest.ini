[pytest]
addopts = -v --ignore=tests/integration --ignore=tests/build --ignore=tests/fixtures
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    env_dependent: marks tests that depend on local machine environment
